{
  "name": "barcomp-web",
  "version": "1.0.0",
  "private": true,
  "description": "BARCOMP — motor JavaScript + visor Three.js. Se compila a un solo HTML offline.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node test_motor.js",
    "check": "node test_motor.js && node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
