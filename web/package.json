{
  "name": "barcomp-web",
  "version": "1.0.0",
  "private": true,
  "description": "BARCOMP — motor JavaScript + visor Three.js. Se compila a un solo HTML offline.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node test_motor.js",
    "test:ui": "node tools/ui_test.mjs",
    "demo:amarre": "node tools/demo_amarre.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build && npm run test:ui"
  },
  "devDependencies": {
    "@types/three": "0.185.4",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "three": "0.185.1"
  }
}
